{block #flashes}
    {if count($flashes) > 0}
        <div class="row">
        {foreach $flashes as $flash}
            <div class="alert alert-{if $flash->type}{$flash->type}{else}success{/if} alert-dismissable">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                {$flash->message|noescape}
            </div>
        {/foreach}
        </div>
    {/if}
{/block}